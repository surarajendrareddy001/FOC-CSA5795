#include<stdio.h>
int main(){
	int firstvalue,secondvalue,subtraction;
	printf("enter firstvalue:");
	scanf("%d",&firstvalue);
	printf("enter second value:");
	scanf("%d",&secondvalue);
	subtraction=firstvalue-secondvalue;
	printf("the subtraction of two numbers is %d",subtraction);
}
/* to add two numbers which are greater than zero first of all we should prefer the first value and then we should proceed with the second value */